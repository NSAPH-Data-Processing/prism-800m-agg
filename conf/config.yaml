defaults:
  - _self_
  - datapaths: cannon_prism
  - prism
  - shapefiles: zcta

# == aggregation args
year: 2000
polygon_name: ${hydra:runtime.choices.shapefiles}
shapefile_year: 2020

show_progress: true
plot_output: false

hydra:
  run:
    dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
